# Deny ALL direct web access to this directory (framework internals only).
Require all denied
<IfModule mod_php.c>
    php_flag engine off
</IfModule>
<IfModule mod_php7.c>
    php_flag engine off
</IfModule>
